---
owner_approved: null
---

# Design-System — <App>

> Teil des Detailkonzepts (**UI-Domäne**, nur bei UI-Projekten). Geschrieben vom `designer` im **Vorschlags-Modus** (`docs/specs/design-owner-approval.md`): Entwurf bis zur expliziten Owner-Freigabe (`owner_approved` bleibt `null`), danach bindend für den `coder`. Freigabe stempelt der `designer` als `owner_approved: <ISO-8601-Zeitstempel>` — kein anderer Schreiber. Konformität (Kontrast/Spacing/A11y) prüft der `reviewer` via UI-Pack-Checklist.

## Tokens
<Farbe (inkl. Dark-Mode), Spacing-Skala (8-pt), Typografie.>

## Komponenten-Patterns
<Wiederkehrende UI-Bausteine + Verhalten.>

## Responsive / Breakpoints
<Verhalten je Breite.>

## Accessibility (WCAG 2.1 AA)
<Kontrast (berechnet, nicht geschätzt), sichtbarer Fokus, Tastatur-Navigation, Touch-Targets ≥ 44–48 px.>
